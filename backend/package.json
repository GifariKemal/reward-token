{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.12.34",
    "viem": "^2.55.10"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "seed:export": "bun run seed.ts export",
    "seed:import": "bun run seed.ts import"
  }
}
